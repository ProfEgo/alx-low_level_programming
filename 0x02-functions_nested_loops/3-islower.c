#include "main.h"

/**
 * _islower - Entry point
 * @c: Entry value
 * Description: _islower print lowercase alphabet
 * Descript: intergral 'c'
 * Return: 0
 */

int _islower(int c)
{

if (c < 'a' && c > 'z')
{
return (0);
}

else
{
ruturn(0);
}

_putchar('\n');
}
