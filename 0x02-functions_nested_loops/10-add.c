#include "main.h"

/**
 * add - value absolute
 * @p: initialization of the addition of values
 * @m: first int to print
 * @n: second int to print
 * Return: Always 0.
 */

int add(int m, int n)
{
int p;
p = m + n;
_putchar(p + '0');
return (0);

}
