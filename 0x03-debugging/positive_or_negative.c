#include "main.h"

/**
 * positive_or_negative - tests function that
 * @i: the int to be returned
 *
 * Description: To check positive & negative
 * Return: 1 if positive. 0 otherwise
 */

void positive_or_negative(int i)
{

if (i > 0)
{
	printf("%d is positive\n", i);
}

else if (i == 0)
{
	printf("%d is positive\n", i);
}

else
{
	printf("%d is positive\n", i);
}


}
